# from datetime import datetime

# # Generate global timestamp once
# TIMESTAMP: str = datetime.now().strftime("%Y_%m_%d_%H_%M_%S")

# # Base artifact root directory with timestamp
# ARTIFACT_DIR = f"artifacts/{TIMESTAMP}"